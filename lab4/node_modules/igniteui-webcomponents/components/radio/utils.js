import { iterNodes } from '../common/util.js';
export function getGroup(member) {
    const iterator = iterNodes(document.body, 'SHOW_ELEMENT', (radio) => radio.matches(`${member.tagName}[name='${member.name}']`));
    const result = {
        active: [],
        checked: [],
        radios: [],
        siblings: [],
    };
    for (const each of iterator) {
        result.radios.push(each);
        if (!each.disabled) {
            result.active.push(each);
        }
        if (each.checked) {
            result.checked.push(each);
        }
        if (!each.isSameNode(member)) {
            result.siblings.push(each);
        }
    }
    return result;
}
//# sourceMappingURL=utils.js.map